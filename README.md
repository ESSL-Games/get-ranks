# get-ranks

This is a simple application to get the ranks & peak ranks of all players that participate in the Valorant ESSL Champions Cup powered by MCI 

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run dev
```
